<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/background"
    tools:context="com.example.yunalescca.swedishpronunciations.controllers.MainActivity">

    <Button
        android:text="AIUEO"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/letter_size_main_menu"
        android:id="@+id/mainView_btn_vowel"
        android:onClick="vocalMenuGoTo"
        android:layout_marginTop="15dp"
        android:layout_centerInParent="true"
        android:layout_alignParentTop="true"
        android:background="@drawable/button1" />

    <Button
        android:text="Achievements"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/letter_size_main_menu"
        android:id="@+id/mainView_btn_achievements"
        android:layout_centerInParent="true"
        android:onClick="achievementListGoTo"
        android:background="@drawable/button1"
        android:layout_below="@+id/mainView_btn_vowel"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp" />

    <Button
        android:text="About"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/mainView_btn_about"
        android:onClick="aboutGoTo"
        android:layout_centerInParent="true"
        android:textSize="@dimen/letter_size_main_menu"
        android:background="@drawable/button1"
        android:layout_marginTop="15dp"
        android:layout_alignParentBottom="true"
        android:elevation="0dp" />

</RelativeLayout>
